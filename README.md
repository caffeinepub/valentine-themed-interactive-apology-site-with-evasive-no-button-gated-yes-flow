## Valentine-themed interactive apology site with evasive No button, gated Yes flow, music, photo modal, apology letter, and celebration effects

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
